[workspace]
members = [
    "example_contract",
    "example_contract_permissioned",
    "starstream_cli",
    "starstream_compiler",
    "starstream_sys",
    "starstream_vm",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
